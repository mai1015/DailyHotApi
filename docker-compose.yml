version: "3.8"

services:
  api:
    build:
      context: .
      target: runner
    ports:
      - "6688:6688"
    volumes:
      - "./logs:/app/logs"
    environment:
      - PORT=6688
    user: "mai1015"
    restart: always
